# Leave empty to use the Vite dev proxy (recommended for local development).
# Set to the deployed API origin in production, e.g. https://api.fotoin.id
VITE_API_BASE_URL=

# Token for the reviewer console at /review. Must match REVIEWER_TOKEN on the server.
VITE_REVIEWER_TOKEN=dev-reviewer-token
